# Batch review: W10 SP drift-policy loop run (2026-08-23)

Gate journal for the /loop-drive run of `docs/plans/2026-08-23.W10.sp-drift-policy-plan.md`.
Autonomy mode: auto (session), set before the run started.
BATCH and DEFAULT entries are the review obligation; ASK and STOP entries are record-only.

## Entries

### 1. ASK (record-only) - consolidated pre-autonomy ask

- Decision: on `set auto`, nothing was outstanding (no pending offers, route choices, or commits); autonomy commenced immediately.
- Rationale: the skill requires the consolidated ask before autonomy governs; the outstanding set was empty.
- Reversal: n/a - resolved live.

### 2. BATCH - Step 0 topology lean

- Decision: single-orchestrator, 5-wave shape mirroring the plan's dependency graph; one topology diagrammed, not two.
- Rationale: the dependency graph in the source plan dictates the wave structure outright; no alternate shape came within 60/40.
- Reversal: scoped re-derivation of the wave graph with an alternate packing (e.g. merging single-unit waves 2-4 into serial units of one manifest) - a judgment re-run, not a fact fix.

### 3. STOP (record-only) - corpora premise

- Decision: the plan's "committed corpora" premise is false (corpora/ gitignored, 0 tracked files, 28M on disk); Jeremy chose accept-on-disk plus `chmod -R a-w` on both pinned corpus dirs instead of committing them.
- Rationale: he does not edit the pinned corpora; read-only protects them without a 28M git commit; his customizations live in a different set.
- Reversal: n/a - resolved live. Premise deviation recorded: a fresh clone must re-acquire corpora before re-running the molt.

### 4. DEFAULT - Step 7 execution-details ask auto-taken

- Decision: the "see execution details before launch?" ask auto-took its default; the dashboard, topology, and watch points were delivered in the STOP report anyway.
- Rationale: knob at auto; the gate is DEFAULT-class and its informational content reached Jeremy in the same turn.
- Reversal: cheap - Jeremy can request a dry run or dashboard at any gate.

### 5a. BATCH - wave 1 gate: pycache patch exclusion

- Decision: T1's patch carried a binary `scripts/__pycache__/*.pyc` (worker `git add -A`; repo `.gitignore` lacks `__pycache__/`); applied with `git apply --exclude='*__pycache__*'` at the gate, and all future gates apply patches the same way. Manifests left untouched.
- Rationale: orchestrator-side handling touches zero specs (the multi-unit spec-edit alternative would be STOP-class); the pyc is generated noise, not deliverable.
- Reversal: cheap - re-apply any patch without the exclude. Repo-hygiene suggestion (add `__pycache__/` to .gitignore) flagged for Jeremy at the end, not taken (out of scope).

### 5a1. BATCH - T8 criterion-7 failure attributed as source-plan check bug

- Decision: the verification script's `findings` regex counted 8 prompt-body `### ` headings (373 vs true 365); fence count 2190 = 6 x 365 exactly, so the report is correct and the script is wrong. Attributed per the plan's own gap-2 attribution rule; recorded for the review issue; report not doubted.
- Rationale: verified by a fence-aware recount (365 findings outside fences, exactly 3 fenced blocks each).
- Reversal: n/a for the report; the script correction is a one-line regex fix recorded for the review issue.

### 5a2. BATCH - final-wave advisory loop-review launched

- Decision: `/loop-review 10a00e8` run from `loop/sp-drift-policy` after advancement, per the plan's advisory terminal review; findings recorded at the T8 checkpoint.
- Rationale: advisory and non-blocking by design; per-unit validators already gated correctness.
- Reversal: n/a - read-only review.

### 5a3. STOP (pending) - T8 human checkpoint reached

- Decision: run complete through criteria 1-7; report generated (REVIEW=368: 365 classification + 3 retro flags); commit and issue-open staged for Jeremy, never fired.
- Rationale: plan-mandated human checkpoint (criterion 8 spot-check); both staged commands are hard to reverse (a commit, a real GitHub issue).
- Reversal: n/a - Jeremy's trigger.

### 5b. DEFAULT - commit the compiled loop plan before wave 1

- Decision: commit docs/plans/2026-08-23.W10.sp-drift-policy-plan_loop.md on w10-sp-drift-policy-plan before branching the integration branch.
- Rationale: the prior compiled loop plan is tracked (house convention); worktrees branch from committed state and the resume prompt points at this file.
- Reversal: git revert of the single commit.
